/* ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   SalesKit Pro v3.1 — Premium UI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */
:root{
  --bg:#04040d;
  --s1:#08081a;
  --s2:#0d0d23;
  --s3:#13132e;
  --border:rgba(255,255,255,.07);
  --border2:rgba(255,255,255,.13);
  --cyan:#22d3ee;
  --purple:#9333ea;
  --pink:#f472b6;
  --green:#34d399;
  --amber:#fbbf24;
  --red:#f87171;
  --indigo:#818cf8;
  --text:#eef2ff;
  --muted:#64748b;
  --dim:#1e293b;
  --font:'DM Sans',-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;
  --head:'Syne','DM Sans',-apple-system,sans-serif;
  --glass:rgba(255,255,255,.03);
  --glass-b:rgba(255,255,255,.07);
  --gp:rgba(147,51,234,.35);
  --gc:rgba(34,211,238,.35);
  --shadow:0 8px 32px rgba(0,0,0,.5);
  --shadow-lg:0 20px 60px rgba(0,0,0,.6);
  --radius:14px;
}

*{margin:0;padding:0;box-sizing:border-box}
body{font-family:var(--font);background:var(--bg);color:var(--text);min-height:100vh;overflow-x:hidden}

/* ── SIDEBAR ─────────────────────────────────────────────── */
.shell{display:flex;height:100vh}

.sidebar{
  width:230px;
  background:linear-gradient(180deg,rgba(147,51,234,.04) 0%,rgba(8,8,26,.98) 40%,var(--s1) 100%);
  border-right:1px solid var(--border);
  display:flex;flex-direction:column;flex-shrink:0;position:relative;overflow:hidden;
}
.sidebar::before{
  content:'';position:absolute;top:-100px;left:-60px;
  width:260px;height:260px;
  background:radial-gradient(circle,rgba(147,51,234,.18) 0%,transparent 70%);
  pointer-events:none;
}
.sidebar::after{
  content:'';position:absolute;bottom:-60px;right:-60px;
  width:180px;height:180px;
  background:radial-gradient(circle,rgba(34,211,238,.08) 0%,transparent 70%);
  pointer-events:none;
}

.logo{padding:22px 20px;border-bottom:1px solid var(--border)}
.logo-mark{
  font-family:var(--head);font-size:20px;font-weight:800;
  letter-spacing:-1px;display:flex;align-items:center;gap:9px;
}
.logo-mark .dot{
  width:9px;height:9px;border-radius:50%;
  background:var(--cyan);
  box-shadow:0 0 12px var(--cyan),0 0 24px rgba(34,211,238,.4);
}
.logo-sub{font-size:10px;color:var(--muted);margin-top:3px;font-family:monospace;letter-spacing:2.5px;text-transform:uppercase}

nav{flex:1;padding:10px 0;overflow-y:auto}
.nav-section{font-size:9px;font-weight:700;letter-spacing:2.5px;color:rgba(255,255,255,.15);padding:18px 20px 6px;text-transform:uppercase}
.nav-btn{
  display:flex;align-items:center;gap:10px;padding:10px 20px;
  cursor:pointer;font-size:13px;font-weight:500;color:var(--muted);
  border-left:2px solid transparent;transition:all .2s;user-select:none;position:relative;
}
.nav-btn:hover{color:var(--text);background:rgba(255,255,255,.04)}
.nav-btn.on{
  color:#fff;
  border-left-color:var(--cyan);
  background:linear-gradient(90deg,rgba(34,211,238,.1) 0%,transparent 80%);
}
.nav-btn.on::before{
  content:'';position:absolute;left:0;top:0;bottom:0;width:2px;
  background:linear-gradient(180deg,var(--cyan),var(--purple));
  box-shadow:0 0 8px var(--cyan);
}
.nav-btn .ni{width:18px;text-align:center;font-size:15px}
.nav-btn .nbadge{
  margin-left:auto;background:linear-gradient(135deg,var(--pink),var(--purple));
  color:#fff;font-size:9px;font-weight:700;padding:2px 8px;border-radius:20px;
}

.sidebar-footer{padding:14px 20px;border-top:1px solid var(--border);font-size:11px;color:var(--muted)}
.user-row{display:flex;align-items:center;gap:8px;margin-bottom:6px}
.user-av{
  width:28px;height:28px;border-radius:50%;
  background:linear-gradient(135deg,var(--purple),var(--cyan));
  display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:#fff;
  box-shadow:0 0 12px rgba(147,51,234,.4);
}

/* ── MAIN AREA ──────────────────────────────────────────── */
.main{flex:1;overflow-y:auto;display:flex;flex-direction:column}

.topbar{
  background:rgba(8,8,26,.85);
  backdrop-filter:blur(16px);
  -webkit-backdrop-filter:blur(16px);
  border-bottom:1px solid var(--border);
  padding:14px 28px;display:flex;align-items:center;justify-content:space-between;
  position:sticky;top:0;z-index:20;
}
.topbar-title{font-family:var(--head);font-size:16px;font-weight:700;letter-spacing:-.3px}
.topbar-right{display:flex;align-items:center;gap:12px}

.live-pill{
  display:flex;align-items:center;gap:6px;
  background:rgba(52,211,153,.08);
  border:1px solid rgba(52,211,153,.25);
  border-radius:20px;padding:5px 13px;
  font-size:11px;color:var(--green);font-weight:600;
}
.live-dot{
  width:6px;height:6px;border-radius:50%;background:var(--green);
  box-shadow:0 0 6px var(--green);
  animation:pulse 2s infinite;
}
@keyframes pulse{0%,100%{opacity:1;transform:scale(1)}50%{opacity:.5;transform:scale(1.4)}}

.page{display:none;padding:24px 28px;flex:1}
.page.on{display:block}

/* ── CARDS ──────────────────────────────────────────────── */
.card{
  background:linear-gradient(135deg,rgba(255,255,255,.04) 0%,rgba(255,255,255,.01) 100%);
  border:1px solid var(--border);
  border-radius:var(--radius);
  overflow:hidden;
  box-shadow:var(--shadow);
  backdrop-filter:blur(8px);
  -webkit-backdrop-filter:blur(8px);
}
.card-hd{
  padding:16px 20px;border-bottom:1px solid var(--border);
  display:flex;align-items:center;justify-content:space-between;
  background:rgba(255,255,255,.02);
}
.card-hd-title{font-family:var(--head);font-size:13px;font-weight:700;letter-spacing:-.2px}
.card-body{padding:20px}

/* ── STATS ──────────────────────────────────────────────── */
.stats-row{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-bottom:22px}
.stat{
  border:1px solid var(--border);
  border-radius:var(--radius);padding:20px;
  position:relative;overflow:hidden;
  background:linear-gradient(135deg,rgba(255,255,255,.04) 0%,rgba(255,255,255,.01) 100%);
  box-shadow:var(--shadow);
  transition:transform .2s,box-shadow .2s;
}
.stat:hover{transform:translateY(-3px);box-shadow:0 16px 48px rgba(0,0,0,.6)}
.stat::after{content:'';position:absolute;top:0;left:0;right:0;height:1px}
.stat.c0::after{background:linear-gradient(90deg,var(--cyan),var(--purple))}
.stat.c1::after{background:linear-gradient(90deg,var(--green),var(--cyan))}
.stat.c2::after{background:linear-gradient(90deg,var(--pink),var(--amber))}
.stat.c3::after{background:linear-gradient(90deg,var(--amber),var(--green))}
.stat.c0{box-shadow:var(--shadow),0 0 40px rgba(34,211,238,.04)}
.stat.c1{box-shadow:var(--shadow),0 0 40px rgba(52,211,153,.04)}
.stat.c2{box-shadow:var(--shadow),0 0 40px rgba(244,114,182,.04)}
.stat.c3{box-shadow:var(--shadow),0 0 40px rgba(251,191,36,.04)}

.stat-lbl{font-size:10px;font-weight:700;letter-spacing:1.8px;text-transform:uppercase;color:var(--muted);margin-bottom:10px}
.stat-val{font-family:var(--head);font-size:32px;font-weight:800;letter-spacing:-1.5px;line-height:1}
.stat-sub{font-size:11px;color:var(--muted);margin-top:6px}
.stat-icon{position:absolute;bottom:14px;right:16px;font-size:30px;opacity:.12}

.dash-grid{display:grid;grid-template-columns:1fr 320px;gap:16px;margin-bottom:16px}

/* ── TABLES ─────────────────────────────────────────────── */
table{width:100%;border-collapse:collapse;font-size:13px}
thead th{font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:var(--muted);padding:0 0 12px;border-bottom:1px solid var(--border)}
tbody td{padding:12px 0;border-bottom:1px solid rgba(255,255,255,.04);vertical-align:middle}
tbody tr:last-child td{border-bottom:none}
tbody tr:hover td{background:rgba(255,255,255,.02)}

.av{
  width:32px;height:32px;border-radius:50%;
  background:linear-gradient(135deg,var(--purple),var(--indigo));
  display:inline-flex;align-items:center;justify-content:center;
  font-size:12px;font-weight:700;color:#fff;flex-shrink:0;
}
.cell-name{display:flex;align-items:center;gap:10px}

/* ── TAGS ───────────────────────────────────────────────── */
.tag{display:inline-flex;align-items:center;gap:4px;padding:3px 10px;border-radius:20px;font-size:10px;font-weight:700;letter-spacing:.5px}
.tag.hot{background:rgba(244,114,182,.12);color:var(--pink);border:1px solid rgba(244,114,182,.25)}
.tag.new{background:rgba(34,211,238,.08);color:var(--cyan);border:1px solid rgba(34,211,238,.2)}
.tag.close{background:rgba(52,211,153,.1);color:var(--green);border:1px solid rgba(52,211,153,.2)}
.tag.cold{background:rgba(100,116,139,.08);color:var(--muted);border:1px solid rgba(100,116,139,.2)}

/* ── FORMS ──────────────────────────────────────────────── */
.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}
.fg{display:flex;flex-direction:column;gap:6px}
.fg.full{grid-column:1/-1}
label{font-size:10px;font-weight:700;letter-spacing:1.5px;text-transform:uppercase;color:var(--muted)}

input,select,textarea{
  background:rgba(255,255,255,.04);
  border:1px solid var(--border);
  border-radius:10px;padding:10px 14px;
  color:var(--text);font-family:var(--font);font-size:13px;
  outline:none;transition:all .2s;
}
input:focus,select:focus,textarea:focus{
  border-color:rgba(34,211,238,.5);
  background:rgba(34,211,238,.04);
  box-shadow:0 0 0 3px rgba(34,211,238,.08);
}
select option{background:var(--s3)}
textarea{resize:vertical;min-height:75px}

/* ── BUTTONS ────────────────────────────────────────────── */
.btn{
  padding:9px 18px;border-radius:10px;font-family:var(--font);
  font-size:13px;font-weight:600;cursor:pointer;border:none;
  transition:all .2s;display:inline-flex;align-items:center;gap:7px;
  position:relative;overflow:hidden;
}
.btn::after{
  content:'';position:absolute;inset:0;
  background:linear-gradient(rgba(255,255,255,.08),transparent);
  opacity:0;transition:opacity .2s;
}
.btn:hover::after{opacity:1}

.btn-cyan{
  background:linear-gradient(135deg,var(--cyan),#06b6d4);
  color:#000;font-weight:700;
  box-shadow:0 4px 20px rgba(34,211,238,.25);
}
.btn-cyan:hover{transform:translateY(-1px);box-shadow:0 6px 28px rgba(34,211,238,.4)}
.btn-cyan:active{transform:translateY(0)}

.btn-ghost{
  background:rgba(255,255,255,.04);color:var(--text);
  border:1px solid var(--border);
}
.btn-ghost:hover{border-color:rgba(34,211,238,.4);color:var(--cyan);background:rgba(34,211,238,.06)}

.btn-red{
  background:rgba(248,113,113,.12);color:var(--red);
  border:1px solid rgba(248,113,113,.25);
}
.btn-red:hover{background:rgba(248,113,113,.2);box-shadow:0 4px 16px rgba(248,113,113,.2)}

.btn-purple{
  background:linear-gradient(135deg,var(--purple),#7c3aed);
  color:#fff;
  box-shadow:0 4px 20px rgba(147,51,234,.3);
}
.btn-purple:hover{transform:translateY(-1px);box-shadow:0 6px 28px rgba(147,51,234,.5)}

.btn-green{
  background:linear-gradient(135deg,var(--green),#059669);
  color:#000;font-weight:700;
  box-shadow:0 4px 16px rgba(52,211,153,.25);
}
.btn-amber{
  background:linear-gradient(135deg,var(--amber),#d97706);
  color:#000;font-weight:700;
}

.fa{display:flex;gap:8px;margin-top:10px;grid-column:1/-1}

/* ── ACTIVITY LOG ───────────────────────────────────────── */
.act-item{display:flex;gap:12px;padding:10px 0;border-bottom:1px solid rgba(255,255,255,.04);align-items:flex-start}
.act-item:last-child{border-bottom:none}
.act-dot{width:7px;height:7px;border-radius:50%;margin-top:5px;flex-shrink:0;box-shadow:0 0 6px currentColor}
.act-txt{font-size:12.5px;line-height:1.6;flex:1}
.act-time{font-size:10px;color:var(--muted);font-family:monospace;white-space:nowrap}

/* ── CODE EMBED BOX ─────────────────────────────────────── */
.embed-box{
  background:rgba(0,0,0,.4);
  border:1px solid var(--border);
  border-radius:12px;padding:16px;
  font-family:'JetBrains Mono',monospace;font-size:11px;color:#94a3b8;
  line-height:1.8;overflow-x:auto;position:relative;
  max-height:280px;overflow-y:auto;
}
.copy-btn{
  position:sticky;top:0;float:right;
  background:rgba(147,51,234,.2);
  border:1px solid rgba(147,51,234,.3);
  border-radius:6px;padding:3px 10px;
  font-size:10px;font-weight:700;cursor:pointer;
  color:var(--indigo);font-family:var(--font);
  transition:all .2s;
}
.copy-btn:hover{background:rgba(147,51,234,.35);border-color:var(--purple)}

/* ── CHANNEL / PRODUCT CARDS ────────────────────────────── */
.channel-card{
  background:rgba(255,255,255,.03);
  border:1px solid var(--border);
  border-radius:var(--radius);padding:16px;
  transition:border-color .2s,background .2s;
}
.channel-card:hover{border-color:var(--border2);background:rgba(255,255,255,.05)}

.prod-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;margin-bottom:20px}
.prod-card{
  background:linear-gradient(135deg,rgba(255,255,255,.04) 0%,rgba(255,255,255,.01) 100%);
  border:1px solid var(--border);
  border-radius:var(--radius);padding:18px;
  position:relative;transition:all .2s;
  box-shadow:var(--shadow);
}
.prod-card:hover{transform:translateY(-4px);box-shadow:0 16px 48px rgba(0,0,0,.5);border-color:var(--border2)}
.prod-emoji{font-size:34px;margin-bottom:10px;display:block}
.prod-name{font-size:14px;font-weight:700;margin-bottom:4px;letter-spacing:-.2px}
.prod-price{font-size:20px;font-weight:800;font-family:var(--head);color:var(--cyan);margin-bottom:6px}
.prod-desc{font-size:11.5px;color:var(--muted);line-height:1.6;margin-bottom:12px}
.prod-tags{display:flex;flex-wrap:wrap;gap:4px;margin-bottom:12px}
.ptag{font-size:9px;padding:2px 8px;border-radius:10px;background:rgba(147,51,234,.15);color:#c084fc;font-weight:700;letter-spacing:.5px}
.prod-actions{display:flex;gap:6px}
.prod-badge{position:absolute;top:10px;right:10px;font-size:9px;font-weight:700;padding:3px 8px;border-radius:10px;letter-spacing:.5px}
.pb-hot{background:rgba(244,114,182,.2);color:var(--pink)}
.pb-new{background:rgba(34,211,238,.12);color:var(--cyan)}

/* ── BOT PREVIEW ─────────────────────────────────────────── */
.bot-preview-wrap{display:grid;grid-template-columns:1fr 300px;gap:18px}
.bot-phone{
  width:280px;height:560px;
  background:linear-gradient(180deg,#0f0a1e,#0a0a1a);
  border-radius:32px;border:1px solid rgba(255,255,255,.1);
  overflow:hidden;box-shadow:0 24px 80px rgba(0,0,0,.7),0 0 0 1px rgba(255,255,255,.06) inset;
  display:flex;flex-direction:column;
}
.bot-phone-hd{
  padding:13px 16px;
  background:linear-gradient(135deg,rgba(147,51,234,.3),rgba(34,211,238,.1));
  display:flex;align-items:center;gap:10px;
}
.bot-phone-av{
  width:34px;height:34px;border-radius:50%;
  background:linear-gradient(135deg,var(--purple),var(--cyan));
  display:flex;align-items:center;justify-content:center;font-size:14px;
  box-shadow:0 0 12px rgba(147,51,234,.5);
}
.bot-msgs{flex:1;padding:12px;overflow-y:auto;display:flex;flex-direction:column;gap:10px;background:rgba(0,0,0,.2)}
.bm{display:flex;gap:7px;max-width:90%}
.bm.user{flex-direction:row-reverse;align-self:flex-end}
.bm-av{
  width:24px;height:24px;border-radius:50%;
  background:linear-gradient(135deg,var(--purple),var(--cyan));
  display:flex;align-items:center;justify-content:center;font-size:10px;flex-shrink:0;
}
.bm-bubble{padding:9px 13px;border-radius:16px;font-size:12px;line-height:1.6}
.bm.bot .bm-bubble{
  background:rgba(255,255,255,.06);
  border:1px solid rgba(255,255,255,.08);
  border-top-left-radius:4px;
}
.bm.user .bm-bubble{
  background:linear-gradient(135deg,var(--purple),#7c3aed);
  border-top-right-radius:4px;
  box-shadow:0 4px 16px rgba(147,51,234,.3);
}
.bm-time{font-size:9px;color:rgba(255,255,255,.25);margin-top:3px;font-family:monospace}
.bm.user .bm-time{text-align:right}
.quick-btns{display:flex;flex-wrap:wrap;gap:5px;padding:6px 12px}
.qb{
  background:rgba(34,211,238,.08);
  border:1px solid rgba(34,211,238,.25);
  border-radius:16px;padding:5px 11px;
  font-size:10px;color:var(--cyan);cursor:pointer;white-space:nowrap;
  transition:all .2s;
}
.qb:hover{background:rgba(34,211,238,.15);border-color:var(--cyan)}
.bot-input-row{padding:10px 12px;background:rgba(0,0,0,.3);display:flex;gap:6px;border-top:1px solid rgba(255,255,255,.06)}
.bi{
  flex:1;background:rgba(255,255,255,.07);
  border:1px solid rgba(255,255,255,.1);
  border-radius:20px;padding:7px 14px;font-size:11px;color:#fff;font-family:var(--font);outline:none;
}
.bs{
  background:linear-gradient(135deg,var(--purple),#7c3aed);
  border:none;border-radius:50%;width:30px;height:30px;color:#fff;
  cursor:pointer;font-size:12px;flex-shrink:0;
  display:flex;align-items:center;justify-content:center;
  box-shadow:0 4px 12px rgba(147,51,234,.4);
}

/* ── RULES ──────────────────────────────────────────────── */
.rule-row{
  background:rgba(255,255,255,.02);
  border:1px solid var(--border);
  border-radius:10px;padding:13px 16px;
  display:flex;align-items:center;gap:12px;margin-bottom:8px;
  transition:border-color .2s;
}
.rule-row:hover{border-color:var(--border2)}
.rule-kw{
  font-family:'JetBrains Mono',monospace;font-size:11px;
  background:rgba(147,51,234,.15);color:#c084fc;
  padding:3px 10px;border-radius:6px;white-space:nowrap;
  border:1px solid rgba(147,51,234,.2);
}
.rule-arr{color:var(--dim);font-size:18px}
.rule-reply{flex:1;font-size:12.5px}
.rule-cnt{font-size:10px;color:var(--muted);font-family:monospace;white-space:nowrap}

/* ── TOGGLE ─────────────────────────────────────────────── */
.toggle{
  appearance:none;width:38px;height:21px;
  background:rgba(255,255,255,.1);
  border-radius:21px;cursor:pointer;transition:all .25s;
  position:relative;flex-shrink:0;
  border:1px solid rgba(255,255,255,.1);
}
.toggle::after{
  content:'';position:absolute;top:2px;left:2px;
  width:15px;height:15px;background:#fff;border-radius:50%;
  transition:left .25s;box-shadow:0 1px 4px rgba(0,0,0,.4);
}
.toggle:checked{background:var(--green);border-color:var(--green);box-shadow:0 0 10px rgba(52,211,153,.3)}
.toggle:checked::after{left:19px}

/* ── MODALS ─────────────────────────────────────────────── */
.modal-ov{
  position:fixed;inset:0;background:rgba(0,0,0,.8);z-index:80;
  display:none;align-items:center;justify-content:center;
  backdrop-filter:blur(6px);-webkit-backdrop-filter:blur(6px);
}
.modal-ov.on{display:flex}
.modal{
  background:linear-gradient(135deg,var(--s2),var(--s1));
  border:1px solid rgba(255,255,255,.1);
  border-radius:20px;padding:30px;
  width:520px;max-width:90vw;
  box-shadow:0 32px 80px rgba(0,0,0,.7),0 0 0 1px rgba(255,255,255,.04) inset;
}
.modal h3{font-family:var(--head);font-size:17px;font-weight:700;margin-bottom:22px;letter-spacing:-.3px}
.modal-ft{display:flex;gap:8px;justify-content:flex-end;margin-top:22px}

/* ── LOGIN ──────────────────────────────────────────────── */
.login-ov{
  position:fixed;inset:0;
  background:radial-gradient(ellipse 80% 80% at 50% -20%,rgba(147,51,234,.15),transparent),
             radial-gradient(ellipse 60% 60% at 80% 80%,rgba(34,211,238,.07),transparent),
             var(--bg);
  display:flex;align-items:center;justify-content:center;z-index:100;
}
.login-box{
  background:linear-gradient(135deg,rgba(255,255,255,.06) 0%,rgba(255,255,255,.02) 100%);
  border:1px solid rgba(255,255,255,.1);
  border-radius:24px;padding:44px;width:420px;
  position:relative;overflow:hidden;
  box-shadow:0 32px 80px rgba(0,0,0,.6),0 0 0 1px rgba(255,255,255,.04) inset;
  backdrop-filter:blur(20px);
  -webkit-backdrop-filter:blur(20px);
}
.login-box::before{
  content:'';position:absolute;top:-80px;right:-80px;
  width:220px;height:220px;
  background:radial-gradient(circle,rgba(34,211,238,.12),transparent 70%);
  pointer-events:none;
}
.login-box::after{
  content:'';position:absolute;bottom:-80px;left:-80px;
  width:200px;height:200px;
  background:radial-gradient(circle,rgba(147,51,234,.12),transparent 70%);
  pointer-events:none;
}
.login-logo{
  font-family:var(--head);font-size:28px;font-weight:800;
  margin-bottom:6px;letter-spacing:-.5px;
}

/* ── TOAST ──────────────────────────────────────────────── */
.toast-wrap{position:fixed;bottom:22px;right:22px;z-index:999;display:flex;flex-direction:column;gap:8px}
.toast{
  background:rgba(13,13,35,.95);
  border:1px solid var(--border2);
  border-left:3px solid var(--green);
  border-radius:12px;padding:12px 18px;
  font-size:12.5px;min-width:220px;
  display:flex;align-items:center;gap:10px;
  animation:tin .3s cubic-bezier(.34,1.56,.64,1);
  backdrop-filter:blur(16px);
  box-shadow:var(--shadow);
}
.toast.err{border-left-color:var(--red)}
.toast.inf{border-left-color:var(--cyan)}
@keyframes tin{from{transform:translateX(110%);opacity:0}to{transform:none;opacity:1}}

/* ── SCROLLBAR ──────────────────────────────────────────── */
::-webkit-scrollbar{width:4px;height:4px}
::-webkit-scrollbar-track{background:transparent}
::-webkit-scrollbar-thumb{background:rgba(255,255,255,.08);border-radius:10px}
::-webkit-scrollbar-thumb:hover{background:rgba(255,255,255,.15)}

/* ── EMPTY STATE ────────────────────────────────────────── */
.empty{text-align:center;padding:48px;color:var(--muted);font-size:13px}

/* ── PLAN CARDS ─────────────────────────────────────────── */
.plan-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-bottom:22px}
.plan-card{
  background:rgba(255,255,255,.03);
  border:1px solid var(--border);
  border-radius:20px;padding:24px;
  text-align:center;position:relative;
  transition:all .25s;cursor:pointer;
}
.plan-card:hover{border-color:rgba(34,211,238,.3);transform:translateY(-4px);box-shadow:0 16px 48px rgba(0,0,0,.4)}
.plan-card.active{
  border-color:rgba(34,211,238,.5);
  background:rgba(34,211,238,.05);
  box-shadow:0 0 0 1px rgba(34,211,238,.2),0 16px 48px rgba(34,211,238,.06);
}
.plan-card.popular{
  border-color:rgba(147,51,234,.5);
  background:rgba(147,51,234,.05);
}
.plan-badge{
  position:absolute;top:-11px;left:50%;transform:translateX(-50%);
  background:linear-gradient(135deg,var(--purple),#7c3aed);
  color:#fff;font-size:9px;font-weight:700;padding:4px 12px;
  border-radius:20px;white-space:nowrap;
  box-shadow:0 4px 16px rgba(147,51,234,.4);
}
.plan-name{font-family:var(--head);font-size:17px;font-weight:800;margin-bottom:8px}
.plan-price{font-family:var(--head);font-size:32px;font-weight:800;color:var(--cyan);margin-bottom:4px;letter-spacing:-1px}
.plan-per{font-size:10px;color:var(--muted);margin-bottom:16px;letter-spacing:.5px}
.plan-feat{font-size:11.5px;color:var(--muted);line-height:2.2;text-align:left}
.plan-feat li{list-style:none;display:flex;align-items:center;gap:7px}
.plan-feat li::before{content:'✓';color:var(--green);font-weight:800;font-size:12px;flex-shrink:0}

/* ── QR PAYMENT ─────────────────────────────────────────── */
.qr-box{background:rgba(255,255,255,.04);border:1px solid var(--border);border-radius:14px;padding:20px;text-align:center}
.bank-info{background:rgba(34,211,238,.04);border:1px solid rgba(34,211,238,.15);border-radius:12px;padding:16px;font-size:13px;line-height:2}
.bank-info .bi-row{display:flex;justify-content:space-between;align-items:center;border-bottom:1px solid rgba(34,211,238,.08);padding:6px 0}
.bank-info .bi-row:last-child{border-bottom:none}
.bank-info .bi-val{font-weight:700;font-family:monospace;color:var(--cyan)}
.channel-status{display:inline-flex;align-items:center;gap:5px;font-size:11px;font-weight:600;padding:4px 11px;border-radius:20px}
.cs-active{background:rgba(52,211,153,.1);color:var(--green);border:1px solid rgba(52,211,153,.25)}
.cs-inactive{background:rgba(100,116,139,.08);color:var(--muted);border:1px solid rgba(100,116,139,.2)}

.qr-modal{position:fixed;inset:0;background:rgba(0,0,0,.88);z-index:95;display:none;align-items:center;justify-content:center;backdrop-filter:blur(8px)}
.qr-modal.on{display:flex}
.qr-box{background:linear-gradient(135deg,var(--s2),var(--s1));border:1px solid rgba(255,255,255,.1);border-radius:24px;width:490px;max-width:94vw;overflow:hidden;box-shadow:0 32px 80px rgba(0,0,0,.7)}
.qr-head{padding:18px 24px;border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between}
.qr-body{padding:24px;display:flex;gap:20px}
.qr-img-wrap{flex-shrink:0;width:185px;height:185px;border-radius:16px;overflow:hidden;background:#fff;display:flex;align-items:center;justify-content:center;box-shadow:0 8px 24px rgba(0,0,0,.3)}
.qr-img-wrap img{width:100%;height:100%;object-fit:contain}
.qr-info{flex:1}
.qr-row{display:flex;justify-content:space-between;align-items:center;padding:8px 0;border-bottom:1px solid rgba(255,255,255,.05);font-size:12.5px}
.qr-row:last-child{border-bottom:none}
.qr-val{font-weight:700;font-family:monospace;color:var(--cyan);cursor:pointer;display:flex;align-items:center;gap:4px}
.qr-val:hover{text-decoration:underline}
.qr-amount{font-family:var(--head);font-size:28px;font-weight:800;color:var(--cyan);text-align:center;margin:14px 0 4px;letter-spacing:-1px}
.qr-timer{text-align:center;font-size:12px;color:var(--muted);margin-bottom:10px}
.qr-status{padding:14px;border-top:1px solid var(--border);text-align:center}
.qs-waiting{display:flex;align-items:center;justify-content:center;gap:8px;color:var(--muted);font-size:13px}
.qs-paid{background:rgba(52,211,153,.1);border:1px solid rgba(52,211,153,.25);border-radius:12px;padding:14px;color:var(--green);font-size:14px;font-weight:700}
.spin{animation:spin 1s linear infinite;display:inline-block}
@keyframes spin{to{transform:rotate(360deg)}}

.bill-status{display:inline-flex;align-items:center;gap:4px;padding:3px 10px;border-radius:20px;font-size:10px;font-weight:700}
.bs-pending{background:rgba(251,191,36,.12);color:var(--amber);border:1px solid rgba(251,191,36,.25)}
.bs-active{background:rgba(52,211,153,.12);color:var(--green);border:1px solid rgba(52,211,153,.25)}
.bs-cancelled{background:rgba(100,116,139,.08);color:var(--muted);border:1px solid rgba(100,116,139,.2)}
.bs-expired{background:rgba(248,113,113,.1);color:var(--red);border:1px solid rgba(248,113,113,.25)}

/* ── FUNNEL ─────────────────────────────────────────────── */
.funnel-wrap{display:flex;flex-direction:column;align-items:center;gap:4px;padding:20px 0}
.funnel-step{display:flex;align-items:center;gap:14px;width:100%}
.funnel-bar-wrap{flex:1;position:relative}
.funnel-bar{height:54px;border-radius:12px;transition:width .7s cubic-bezier(.4,0,.2,1);display:flex;align-items:center;padding:0 16px;font-weight:700;font-size:15px;color:#fff;min-width:60px}
.funnel-info{width:130px;text-align:right;font-size:12.5px;color:var(--muted)}
.funnel-arrow{text-align:center;font-size:20px;color:var(--dim);padding:2px 0;margin-left:130px}
.funnel-rate{position:absolute;right:12px;top:50%;transform:translateY(-50%);font-size:11px;font-weight:700;opacity:.8}

/* ── TEAM ───────────────────────────────────────────────── */
.role-badge{font-size:10px;font-weight:700;padding:3px 9px;border-radius:20px}
.role-admin{background:rgba(147,51,234,.15);color:#c084fc;border:1px solid rgba(147,51,234,.25)}
.role-agent{background:rgba(34,211,238,.08);color:var(--cyan);border:1px solid rgba(34,211,238,.2)}
.member-card{
  background:rgba(255,255,255,.02);
  border:1px solid var(--border);
  border-radius:var(--radius);padding:14px 16px;margin-bottom:8px;
  cursor:pointer;transition:all .2s;
}
.member-card:hover{border-color:var(--border2);background:rgba(255,255,255,.04)}
.member-card.sel{border-color:rgba(34,211,238,.4);background:rgba(34,211,238,.04)}
.member-meta{display:flex;gap:12px;margin-top:6px;font-size:11px;color:var(--muted);flex-wrap:wrap}
.perf-bar{height:4px;background:var(--border);border-radius:2px;margin-top:6px}
.perf-bar-fill{height:100%;border-radius:2px;background:linear-gradient(90deg,var(--green),var(--cyan));transition:width .6s}
.agent-panel{background:rgba(255,255,255,.02);border:1px solid var(--border);border-radius:var(--radius);overflow:hidden;display:flex;flex-direction:column}
.agent-panel-hd{padding:16px 20px;border-bottom:1px solid var(--border)}
.agent-panel-body{padding:18px 20px;overflow-y:auto;max-height:72vh}
.cust-assign-row{display:flex;align-items:center;gap:8px;padding:9px 0;border-bottom:1px solid rgba(255,255,255,.04)}
.cust-assign-row:last-child{border-bottom:none}
.role-toggle{display:inline-flex;gap:3px;background:var(--bg);border:1px solid var(--border);border-radius:8px;padding:3px}
.role-btn{padding:5px 14px;border-radius:6px;font-size:11px;font-weight:600;border:none;background:transparent;color:var(--muted);cursor:pointer;transition:all .15s}
.role-btn.on-admin{background:rgba(147,51,234,.2);color:#c084fc}
.role-btn.on-agent{background:rgba(34,211,238,.12);color:var(--cyan)}
.bulk-select{width:100%;min-height:88px;background:rgba(255,255,255,.04);border:1px solid var(--border);border-radius:8px;color:var(--text);padding:6px;font-size:12px;outline:none}
.bulk-select option{padding:4px}
.bulk-select option:checked{background:rgba(147,51,234,.25)}

/* ── LOGIN TABS ─────────────────────────────────────────── */
.login-tabs{display:flex;gap:0;background:rgba(0,0,0,.3);border:1px solid var(--border);border-radius:12px;padding:4px;margin-bottom:24px}
.login-tab{flex:1;padding:9px;text-align:center;border-radius:9px;font-size:12px;font-weight:600;cursor:pointer;color:var(--muted);transition:all .2s}
.login-tab.on{background:rgba(255,255,255,.08);color:var(--text);box-shadow:0 1px 6px rgba(0,0,0,.4)}
.reg-link{text-align:center;margin-top:12px;font-size:12px;color:var(--cyan);cursor:pointer;opacity:.8}
.reg-link:hover{opacity:1;text-decoration:underline}

/* ── SUPER ADMIN ────────────────────────────────────────── */
.super-ov{position:fixed;inset:0;background:var(--bg);z-index:500;display:none;flex-direction:column}
.super-ov.on{display:flex}
.super-topbar{
  background:rgba(8,8,26,.9);
  backdrop-filter:blur(16px);
  border-bottom:1px solid var(--border);
  padding:14px 28px;display:flex;align-items:center;justify-content:space-between;flex-shrink:0;
}
.super-body{flex:1;overflow-y:auto;padding:28px;max-width:1100px;margin:0 auto;width:100%}
.shop-row{display:grid;grid-template-columns:1fr 110px 90px 80px 80px 110px 130px;gap:8px;align-items:center;padding:13px 16px;border-bottom:1px solid rgba(255,255,255,.04);font-size:12.5px;transition:background .15s}
.shop-row:hover{background:rgba(255,255,255,.03)}
.shop-row-hd{font-size:10px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:1.5px;padding:8px 16px;border-bottom:1px solid var(--border)}

/* ── CUSTOMER PORTAL ────────────────────────────────────── */
.portal-ov{position:fixed;inset:0;background:var(--bg);z-index:500;display:none;flex-direction:column}
.portal-ov.on{display:flex}
.portal-topbar{background:rgba(8,8,26,.9);backdrop-filter:blur(16px);border-bottom:1px solid var(--border);padding:14px 24px;display:flex;align-items:center;justify-content:space-between}
.portal-body{flex:1;overflow-y:auto;padding:24px;max-width:760px;margin:0 auto;width:100%}
.portal-msg{display:flex;gap:10px;margin-bottom:12px}
.portal-msg.out{flex-direction:row-reverse}
.portal-bubble{max-width:72%;padding:11px 15px;border-radius:16px;font-size:13px;line-height:1.6;background:rgba(255,255,255,.05);border:1px solid rgba(255,255,255,.07)}
.portal-msg.out .portal-bubble{background:rgba(147,51,234,.18);border-color:rgba(147,51,234,.35);color:var(--text)}
.portal-info-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-bottom:20px}
.portal-info-card{background:rgba(255,255,255,.03);border:1px solid var(--border);border-radius:var(--radius);padding:14px}

/* ── KANBAN ─────────────────────────────────────────────── */
.kanban-wrap{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;align-items:start;min-height:60vh}
.kc{
  background:rgba(255,255,255,.02);
  border:1px solid var(--border);
  border-radius:16px;padding:14px;
  min-height:200px;transition:all .2s;
}
.kc.drag-over{background:rgba(34,211,238,.05);border-color:rgba(34,211,238,.3)}
.kc-header{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px;padding-bottom:12px;border-bottom:1px solid rgba(255,255,255,.06)}
.kc-title{font-size:12px;font-weight:700;text-transform:uppercase;letter-spacing:1px}
.kc-count{font-size:11px;font-weight:700;padding:2px 9px;border-radius:20px;background:rgba(255,255,255,.07)}
.kcard{
  background:linear-gradient(135deg,rgba(255,255,255,.05) 0%,rgba(255,255,255,.02) 100%);
  border:1px solid rgba(255,255,255,.07);
  border-radius:12px;padding:13px;margin-bottom:8px;
  cursor:grab;transition:all .2s;position:relative;
  box-shadow:0 2px 12px rgba(0,0,0,.3);
}
.kcard:active{cursor:grabbing}
.kcard.dragging{opacity:.4;transform:scale(.96)}
.kcard:hover{
  border-color:rgba(34,211,238,.3);
  transform:translateY(-3px);
  box-shadow:0 8px 24px rgba(0,0,0,.4),0 0 0 1px rgba(34,211,238,.1);
}
.kcard-name{font-weight:600;font-size:13px;margin-bottom:4px;letter-spacing:-.1px}
.kcard-phone{font-size:11px;color:var(--muted);font-family:monospace;margin-bottom:10px}
.kcard-meta{display:flex;align-items:center;gap:5px;flex-wrap:wrap}
.kcard-score{font-size:10px;font-weight:700;padding:2px 8px;border-radius:10px;background:rgba(251,191,36,.12);color:var(--amber);border:1px solid rgba(251,191,36,.2)}
.kcard-ch{font-size:10px;padding:2px 7px;border-radius:6px;background:rgba(255,255,255,.06);color:var(--muted)}

/* ── INBOX ──────────────────────────────────────────────── */
.inbox-wrap{display:grid;grid-template-columns:300px 1fr;height:calc(100vh - 130px);gap:0;border:1px solid var(--border);border-radius:16px;overflow:hidden;box-shadow:var(--shadow)}
.inbox-list{background:rgba(8,8,26,.8);border-right:1px solid var(--border);overflow-y:auto;display:flex;flex-direction:column}
.inbox-search{padding:12px;border-bottom:1px solid var(--border);flex-shrink:0}
.inbox-search input{width:100%;background:rgba(255,255,255,.05);border:1px solid var(--border);border-radius:10px;padding:8px 13px;font-size:12px;color:var(--text);outline:none;transition:all .2s}
.inbox-search input:focus{border-color:rgba(34,211,238,.4);background:rgba(34,211,238,.04)}
.inbox-conv{padding:13px 15px;cursor:pointer;border-bottom:1px solid rgba(255,255,255,.04);transition:background .15s;display:flex;gap:10px;align-items:flex-start}
.inbox-conv:hover{background:rgba(255,255,255,.04)}
.inbox-conv.active{background:rgba(34,211,238,.05);border-left:2px solid var(--cyan)}
.inbox-av{width:38px;height:38px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:16px;flex-shrink:0;background:rgba(255,255,255,.07)}
.inbox-conv-name{font-size:13px;font-weight:600;margin-bottom:2px}
.inbox-conv-preview{font-size:11px;color:var(--muted);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;max-width:165px}
.inbox-conv-time{font-size:10px;color:var(--dim);font-family:monospace;white-space:nowrap}
.inbox-badge{background:linear-gradient(135deg,var(--pink),var(--purple));color:#fff;font-size:9px;font-weight:700;padding:2px 7px;border-radius:20px;margin-left:auto;flex-shrink:0}
.inbox-main{display:flex;flex-direction:column;background:var(--bg)}
.inbox-header{padding:13px 16px;border-bottom:1px solid var(--border);background:rgba(8,8,26,.8);display:flex;align-items:center;justify-content:space-between;flex-shrink:0}
.inbox-msgs{flex:1;overflow-y:auto;padding:16px;display:flex;flex-direction:column;gap:10px}
.imsg{display:flex;gap:8px;max-width:80%}
.imsg.out{flex-direction:row-reverse;align-self:flex-end}
.imsg-bubble{padding:10px 14px;border-radius:16px;font-size:13px;line-height:1.6}
.imsg.in  .imsg-bubble{background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.07);border-top-left-radius:3px}
.imsg.out .imsg-bubble{background:linear-gradient(135deg,var(--purple),#7c3aed);color:#fff;border-top-right-radius:3px;box-shadow:0 4px 16px rgba(147,51,234,.3)}
.imsg-meta{font-size:9px;color:var(--muted);margin-top:3px;font-family:monospace}
.imsg.out .imsg-meta{text-align:right}
.inbox-reply{padding:12px 16px;border-top:1px solid var(--border);background:rgba(8,8,26,.8);display:flex;gap:8px;flex-shrink:0}
.inbox-reply textarea{flex:1;background:rgba(255,255,255,.05);border:1px solid var(--border);border-radius:12px;padding:10px 14px;font-size:13px;color:var(--text);font-family:var(--font);outline:none;resize:none;min-height:42px;max-height:120px;transition:all .2s}
.inbox-reply textarea:focus{border-color:rgba(34,211,238,.4);background:rgba(34,211,238,.03)}
.inbox-side{width:240px;border-left:1px solid var(--border);background:rgba(8,8,26,.8);overflow-y:auto;display:flex;flex-direction:column;flex-shrink:0}

/* ── TIMELINE ───────────────────────────────────────────── */
.tl-modal{position:fixed;inset:0;background:rgba(0,0,0,.85);z-index:90;display:none;align-items:center;justify-content:center;backdrop-filter:blur(8px)}
.tl-modal.on{display:flex}
.tl-box{background:linear-gradient(135deg,var(--s2),var(--s1));border:1px solid rgba(255,255,255,.1);border-radius:22px;width:630px;max-width:92vw;max-height:85vh;display:flex;flex-direction:column;overflow:hidden;box-shadow:0 32px 80px rgba(0,0,0,.7)}
.tl-head{padding:22px 26px;border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;flex-shrink:0}
.tl-body{flex:1;overflow-y:auto;padding:22px 26px}
.tl-item{display:flex;gap:14px;margin-bottom:22px;position:relative}
.tl-item::before{content:'';position:absolute;left:15px;top:32px;bottom:-22px;width:1px;background:linear-gradient(180deg,rgba(255,255,255,.1),transparent)}
.tl-item:last-child::before{display:none}
.tl-icon{width:32px;height:32px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:14px;flex-shrink:0;z-index:1}
.tl-content{flex:1;padding-top:4px}
.tl-title{font-size:13px;font-weight:600;margin-bottom:3px}
.tl-detail{font-size:12px;color:var(--muted);line-height:1.6}
.tl-time{font-size:10px;color:var(--dim);font-family:monospace;margin-top:4px}

/* ── PAGINATION ─────────────────────────────────────────── */
.pg-wrap{display:flex;align-items:center;justify-content:space-between;padding:14px 20px;border-top:1px solid rgba(255,255,255,.05);font-size:12px;color:var(--muted)}
.pg-btns{display:flex;gap:4px}
.pg-btn{padding:5px 11px;border-radius:8px;border:1px solid var(--border);background:transparent;color:var(--muted);cursor:pointer;font-size:12px;font-family:var(--font);transition:all .15s}
.pg-btn:hover{border-color:rgba(34,211,238,.4);color:var(--cyan)}
.pg-btn.on{background:rgba(34,211,238,.15);color:var(--cyan);border-color:rgba(34,211,238,.4);font-weight:700}
.pg-btn:disabled{opacity:.3;cursor:not-allowed}

/* ── BULK ACTIONS ────────────────────────────────────────── */
.bulk-bar{display:none;align-items:center;gap:10px;padding:9px 14px;background:rgba(147,51,234,.1);border:1px solid rgba(147,51,234,.25);border-radius:10px;margin-bottom:10px;font-size:13px}
.bulk-bar.on{display:flex}
.bulk-count{color:#c084fc;font-weight:700}

/* ── THEME TOGGLE ────────────────────────────────────────── */
.theme-btn{width:36px;height:36px;border-radius:10px;border:1px solid var(--border);background:rgba(255,255,255,.04);cursor:pointer;font-size:16px;display:flex;align-items:center;justify-content:center;transition:all .2s}
.theme-btn:hover{border-color:rgba(34,211,238,.4);background:rgba(34,211,238,.06)}

body.light{
  --bg:#f8faff;--s1:#ffffff;--s2:#f1f5ff;--s3:#e8eeff;
  --border:rgba(0,0,50,.1);--border2:rgba(0,0,50,.18);
  --text:#0f0f2d;--muted:#6b7280;--dim:#d1d5db;
  --glass:rgba(255,255,255,.8);--glass-b:rgba(0,0,0,.06);
}

/* ── LOADING / SKELETON ──────────────────────────────────── */
.btn:disabled{opacity:.5;cursor:not-allowed;transform:none!important}
.page{animation:fadeIn .18s cubic-bezier(.4,0,.2,1)}
@keyframes fadeIn{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
.skeleton{
  background:linear-gradient(90deg,rgba(255,255,255,.04) 25%,rgba(255,255,255,.08) 50%,rgba(255,255,255,.04) 75%);
  background-size:200% 100%;
  animation:shimmer 1.6s infinite;border-radius:8px;
}
@keyframes shimmer{0%{background-position:200% 0}100%{background-position:-200% 0}}
.kbd{display:inline-flex;align-items:center;padding:2px 6px;border:1px solid var(--border2);border-radius:5px;font-size:10px;font-family:monospace;color:var(--muted);background:rgba(255,255,255,.04)}
.tooltip{position:relative}
.tooltip:hover::after{content:attr(data-tip);position:absolute;bottom:calc(100% + 8px);left:50%;transform:translateX(-50%);background:var(--s3);border:1px solid var(--border);border-radius:8px;padding:5px 10px;font-size:11px;white-space:nowrap;z-index:200;color:var(--text);pointer-events:none;box-shadow:var(--shadow)}

/* ── REAL-TIME NOTIF ─────────────────────────────────────── */
.notif-dot{position:absolute;top:6px;right:6px;width:8px;height:8px;border-radius:50%;background:var(--red);animation:pulse 1.5s infinite;box-shadow:0 0 8px var(--red)}
.rtbadge{display:inline-flex;align-items:center;gap:5px;background:rgba(34,211,238,.08);border:1px solid rgba(34,211,238,.2);border-radius:20px;padding:4px 12px;font-size:11px;color:var(--cyan);font-weight:600}
@keyframes ringShake{0%,100%{transform:rotate(0)}20%{transform:rotate(-15deg)}40%{transform:rotate(15deg)}60%{transform:rotate(-10deg)}80%{transform:rotate(10deg)}}
.ringing{animation:ringShake .6s ease}

/* ── HAMBURGER ───────────────────────────────────────────── */
.hamburger{display:none;flex-direction:column;gap:4px;cursor:pointer;padding:6px;border-radius:8px;background:transparent;border:none}
.hamburger span{display:block;width:20px;height:2px;background:var(--text);border-radius:2px;transition:all .25s}
.hamburger.open span:nth-child(1){transform:rotate(45deg) translate(4px,4px)}
.hamburger.open span:nth-child(2){opacity:0}
.hamburger.open span:nth-child(3){transform:rotate(-45deg) translate(4px,-4px)}
.sidebar-overlay{display:none;position:fixed;inset:0;background:rgba(0,0,0,.7);z-index:49;backdrop-filter:blur(4px)}
.sidebar-overlay.on{display:block}

/* ── RESPONSIVE ──────────────────────────────────────────── */
@media(max-width:900px){
  .stats-row{grid-template-columns:repeat(2,1fr)}
  .dash-grid{grid-template-columns:1fr}
  .prod-grid{grid-template-columns:repeat(2,1fr)}
}
@media(max-width:768px){
  .sidebar{position:fixed;left:-250px;top:0;bottom:0;z-index:50;width:230px;transition:left .28s ease;box-shadow:none}
  .sidebar.mobile-open{left:0;box-shadow:8px 0 40px rgba(0,0,0,.6)}
  .sidebar-overlay.on{display:block}
  .shell{flex-direction:column}
  .main{min-height:100vh}
  .topbar{padding:10px 14px;gap:8px}
  .topbar-title{font-size:14px}
  .hamburger{display:flex}
  .live-pill{display:none}
  #rtStatus{display:none}
  #clk{display:none}
  .page{padding:14px}
  .stats-row{grid-template-columns:repeat(2,1fr);gap:10px}
  .stat-val{font-size:24px}
  .dash-grid{grid-template-columns:1fr;gap:12px}
  .card{overflow-x:auto}
  table{min-width:520px}
  .prod-grid{grid-template-columns:repeat(2,1fr);gap:10px}
  .bot-preview-wrap{flex-direction:column!important}
  .form-grid{grid-template-columns:1fr!important}
  .fg.full{grid-column:1}
}
